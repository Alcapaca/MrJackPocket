import javax.swing.*;

public class JAlibi extends JButton {
	public JAlibi(ImageIcon imageIcon) {
        super(imageIcon);
        setBorder(BorderFactory.createEmptyBorder());
        setContentAreaFilled(false);
    }
}

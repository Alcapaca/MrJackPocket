import javax.swing.*;

public class JDistrict extends JButton {
	public JDistrict(ImageIcon imageIcon) {
        super(imageIcon);
        setBorder(BorderFactory.createEmptyBorder());
        setContentAreaFilled(false);
    }
}
